{"id":"parallel-utilities","split":"val","providers":["zai","codex"],"task":"Build two independent utilities in parallel, then check their combined imports. CSV reporting: csv_report.py must expose summarize(text), reading CSV columns service,duration_ms,status and returning a dict service -> {count,duration_ms,errors}; sum integer durations and count any status other than 'ok' as an error, reject negative durations with ValueError, support quoted service names, and return {} for header-only data. Configuration editing: config_edit.py must expose patch_config(text, updates), parsing a JSON object, replacing supplied top-level keys, removing keys whose update is None, preserving unrelated keys, and returning sorted two-space-indented JSON followed by a newline; reject non-object input with ValueError. Keep these workstreams independently owned. Verify each utility and run combined checks after integration. Modify only these two modules; use the standard library.","files":{"csv_report.py":"def summarize(text):\n    raise NotImplementedError\n","config_edit.py":"def patch_config(text, updates):\n    raise NotImplementedError\n"},"allowed_files":["csv_report.py","config_edit.py"],"check":["{python}","-c","import json; from csv_report import summarize; from config_edit import patch_config; nl=chr(10); assert summarize(nl.join(['service,duration_ms,status','a,10,ok','a,5,error','\"b,c\",7,ok','']))=={'a':{'count':2,'duration_ms':15,'errors':1},'b,c':{'count':1,'duration_ms':7,'errors':0}}; assert summarize('service,duration_ms,status'+nl)=={}; result=patch_config('{\"z\":1,\"a\":2,\"drop\":3}',{'a':4,'drop':None}); assert json.loads(result)=={'a':4,'z':1}; assert result.splitlines()==['{','  \"a\": 4,','  \"z\": 1','}']; assert result.endswith(nl)\nfor f,args in [(summarize,('service,duration_ms,status'+nl+'a,-1,ok'+nl,)),(patch_config,('[]',{}))]:\n try: f(*args)\n except ValueError: pass\n else: raise AssertionError('invalid input accepted')"]}
{"id":"blocking-child","split":"test","providers":["zai","codex"],"task":"Ask one child to inspect calc.py and explain whether add(2,3) returns 5. This is one blocking investigation: keep the current trunk and provider. Wait for its answer, verify the result with Python, and report it without changing any files.","files":{"calc.py":"def add(a,b):\n    return a+b\n"},"allowed_files":[],"check":["{python}","-c","from calc import add; assert add(2,3)==5"]}
